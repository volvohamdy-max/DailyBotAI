const fs=require('fs'),path=require('path');
const {getHistoricalRates}=require('dukascopy-node');
const FROM=process.argv[2]||'2025-01-01',TO=process.argv[3]||'2026-08-23';
(async()=>{console.log(`📥 Dukascopy XAUUSD M1 ${FROM} → ${TO}`);const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(FROM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m1',format:'json',volumes:true,batchSize:5,pauseBetweenBatchesMs:250,useCache:true,cacheFolderPath:'./data/dukascopy-cache-m1'});const rows=raw.map(x=>({timestamp:+x.timestamp,open:+x.open,high:+x.high,low:+x.low,close:+x.close,volume:+x.volume||0})).filter(x=>[x.timestamp,x.open,x.high,x.low,x.close].every(Number.isFinite)).sort((a,b)=>a.timestamp-b.timestamp);fs.mkdirSync(path.join(__dirname,'../data'),{recursive:true});const f=path.join(__dirname,'../data/xauusd-m1-dukascopy.json');fs.writeFileSync(f,JSON.stringify(rows));console.log(`✅ Saved ${rows.length} M1 candles → ${f}`)})().catch(e=>{console.error(e);process.exit(1)});
#!/usr/bin/env node
'use strict';
const {getHistoricalRates}=require('dukascopy-node');
const FROM=process.argv[2]||'2025-08-25',TO=process.argv[3]||'2026-08-25',CACHE='./data/dukascopy-cache';
const C={lookback:6,sweepAtr:.04,upperWickMin:.60,priorMoveAtr:.50,start:9,end:13,tp:5,sl:5,maxBars:4};
function atr(c,p=14){const o=Array(c.length).fill(null),q=[];let s=0;for(let i=1;i<c.length;i++){const pc=c[i-1].close,tr=Math.max(c[i].high-c[i].low,Math.abs(c[i].high-pc),Math.abs(c[i].low-pc));q.push(tr);s+=tr;if(q.length>p)s-=q.shift();if(q.length===p)o[i]=s/p}return o}
function stats(t){let eq=0,peak=0,dd=0,g=0,l=0,w=0,ls=0,maxLs=0;for(const x of t){eq+=x.r;peak=Math.max(peak,eq);dd=Math.max(dd,peak-eq);if(x.r>0){w++;g+=x.r;ls=0}else{l-=x.r;ls++;maxLs=Math.max(maxLs,ls)}}return{n:t.length,wr:t.length?w/t.length*100:0,net:eq,pf:l?g/l:(g?999:0),dd,ls:maxLs}}
function fmt(s){return`${s.n} trades | WR ${s.wr.toFixed(1)}% | Net ${s.net>=0?'+':''}${s.net.toFixed(2)}R | PF ${s.pf.toFixed(2)} | DD ${s.dd.toFixed(2)}R | LS ${s.ls}`}
(async()=>{console.log('🌊 GOLD SWEEP 5 — LIVE EXACT BACKTEST');console.log(`📅 ${FROM} → ${TO}`);console.log('SELL only | LB6 | sweep 0.04ATR | wick>=60% | prior move>=0.5ATR | UTC 09-13 | TP $5 / SL $5 | max 20m');
const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(FROM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m5',format:'json',priceType:'bid',volumes:true,useCache:true,cacheFolderPath:CACHE});const c=(raw||[]).map(x=>({timestamp:+x.timestamp,open:+x.open,high:+x.high,low:+x.low,close:+x.close})).filter(x=>[x.timestamp,x.open,x.high,x.low,x.close].every(Number.isFinite)).sort((a,b)=>a.timestamp-b.timestamp),A=atr(c);console.log(`✅ M5 candles: ${c.length}`);const T=[];
for(let i=30;i<c.length-C.maxBars-1;i++){const a=A[i],b=c[i];if(!(a>0))continue;const h=new Date(b.timestamp).getUTCHours();if(h<C.start||h>=C.end)continue;let priorHigh=-Infinity;for(let k=i-C.lookback;k<i;k++)priorHigh=Math.max(priorHigh,c[k].high);const range=b.high-b.low;if(!(range>0))continue;const upperWick=(b.high-Math.max(b.open,b.close))/range,priorMove=Math.abs(c[i-1].close-c[i-3].close);if(b.high<priorHigh+a*C.sweepAtr||!(b.close<priorHigh)||!(b.close<b.open)||upperWick<C.upperWickMin||priorMove<a*C.priorMoveAtr)continue;const entry=c[i+1].open,tp=entry-C.tp,sl=entry+C.sl;let r=null,bars=C.maxBars;for(let j=i+1;j<=i+C.maxBars;j++){const hitSL=c[j].high>=sl,hitTP=c[j].low<=tp;if(hitSL||hitTP){r=hitSL?-1:1;bars=j-i;break}}if(r===null){const px=c[i+C.maxBars].close;r=Math.max(-1,Math.min(1,(entry-px)/C.sl))}T.push({time:c[i+1].timestamp,side:'SELL',r,bars});i+=Math.max(0,bars-1)}
console.log('\nALL  '+fmt(stats(T)));const cut=Math.floor(T.length*.7);console.log('DEV  '+fmt(stats(T.slice(0,cut))));console.log('OOS  '+fmt(stats(T.slice(cut))));console.log('\n📌 Same-bar TP+SL conflict = SL. Entry = next M5 open; unresolved after 20m marked-to-market capped ±1R.');})().catch(e=>{console.error(e);process.exit(1)});

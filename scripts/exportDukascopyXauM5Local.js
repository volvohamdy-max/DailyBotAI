const fs=require('fs'),path=require('path');
const {getHistoricalRates}=require('dukascopy-node');
const FROM=process.argv[2]||'2024-08-23',TO=process.argv[3]||'2026-08-23';
(async()=>{console.log(`📥 Dukascopy XAUUSD M5 ${FROM} → ${TO}`);const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(FROM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m5',format:'json',volumes:true,batchSize:10,pauseBetweenBatchesMs:200,useCache:true,cacheFolderPath:'./data/dukascopy-cache'});const rows=raw.map(x=>({timestamp:+x.timestamp,open:+x.open,high:+x.high,low:+x.low,close:+x.close,volume:+x.volume||0})).filter(x=>[x.timestamp,x.open,x.high,x.low,x.close].every(Number.isFinite)).sort((a,b)=>a.timestamp-b.timestamp);fs.mkdirSync(path.join(__dirname,'../data'),{recursive:true});const f=path.join(__dirname,'../data/xauusd-m5-dukascopy.json');fs.writeFileSync(f,JSON.stringify(rows));console.log(`✅ Saved ${rows.length} candles → ${f}`)})().catch(e=>{console.error(e);process.exit(1)});

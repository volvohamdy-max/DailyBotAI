#!/usr/bin/env node
'use strict';
const {getHistoricalRates}=require('dukascopy-node');
const FROM=process.argv[2]||'2025-08-25',TO=process.argv[3]||'2026-08-25',CACHE='./data/dukascopy-cache',TP=5,SL=5;
function atr(c,p=14){const o=Array(c.length).fill(null),q=[];let s=0;for(let i=1;i<c.length;i++){const pc=c[i-1].close,tr=Math.max(c[i].high-c[i].low,Math.abs(c[i].high-pc),Math.abs(c[i].low-pc));q.push(tr);s+=tr;if(q.length>p)s-=q.shift();if(q.length===p)o[i]=s/p}return o}
function st(t){let e=0,p=0,d=0,w=0,l=0,ls=0,ml=0;for(const x of t){e+=x.r;p=Math.max(p,e);d=Math.max(d,p-e);if(x.r>0){w++;ls=0}else{l++;ls++;ml=Math.max(ml,ls)}}return{n:t.length,wr:t.length?100*w/t.length:0,net:e,pf:l?w/l:(w?999:0),dd:d,ls:ml}}
function F(s){return`${s.n} trades | WR ${s.wr.toFixed(1)}% | Net ${s.net>=0?'+':''}${s.net.toFixed(0)}R | PF ${s.pf.toFixed(2)} | DD ${s.dd.toFixed(0)}R | LS ${s.ls}`}
function hit(c,i,maxBars=4){const e=c[i+1].open,tp=e-TP,sl=e+SL;for(let j=i+1;j<=Math.min(c.length-1,i+maxBars);j++){const hs=c[j].high>=sl,ht=c[j].low<=tp;if(hs||ht)return{r:hs?-1:1,bars:j-i}}return null}
(async()=>{console.log('⚡ GOLD SWEEP 5 SELL — PRACTICAL VALIDATION');console.log(`📅 ${FROM} → ${TO} | $5/$5 | SELL | simple price action`);const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(FROM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m5',format:'json',priceType:'bid',volumes:true,useCache:true,cacheFolderPath:CACHE});const c=raw.map(x=>({timestamp:+x.timestamp,open:+x.open,high:+x.high,low:+x.low,close:+x.close})).sort((a,b)=>a.timestamp-b.timestamp),A=atr(c),R=[];
// Keep only the structural rules that mattered in Round 2. No RSI/ADX/AI/score and no micro-optimization grid.
const grid=[];for(const lb of [4,5,6])for(const sweep of [.02,.04,.06])for(const wick of [.55,.60,.65])for(const prior of [.5,.75,1])for(const session of [[8,14],[9,14],[9,13]])grid.push({lb,sweep,wick,prior,session});console.log(`⚡ Practical configs: ${grid.length}`);
for(const x of grid){const T=[];for(let i=30;i<c.length-6;i++){if(!Number.isFinite(A[i]))continue;const hr=new Date(c[i].timestamp).getUTCHours();if(hr<x.session[0]||hr>=x.session[1])continue;const b=c[i],rg=b.high-b.low;if(rg<=0)continue;let hi=-Infinity;for(let k=i-x.lb;k<i;k++)hi=Math.max(hi,c[k].high);const uw=(b.high-Math.max(b.open,b.close))/rg,prev=Math.abs(c[i-1].close-c[i-3].close);if(!(b.high>=hi+A[i]*x.sweep&&b.close<hi&&b.close<b.open&&uw>=x.wick&&prev>=A[i]*x.prior))continue;const o=hit(c,i,4);if(o){T.push({time:c[i+1].timestamp,r:o.r});i+=Math.max(0,o.bars-1)}}if(T.length<35)continue;const n=T.length,a=Math.floor(n/3),b=Math.floor(2*n/3),all=st(T),s1=st(T.slice(0,a)),s2=st(T.slice(a,b)),s3=st(T.slice(b));const pass=all.n>=60&&all.wr>=58&&all.pf>=1.35&&all.dd<=8&&s1.net>=0&&s2.net>=0&&s3.net>=0;const score=all.net+Math.min(all.n,150)*.08+Math.min(all.pf,3)*8-all.dd*1.5+Math.min(s1.net,s2.net,s3.net);R.push({...x,T,all,s1,s2,s3,pass,score})}
R.sort((a,b)=>b.score-a.score);console.log('\n🏆 TOP 12 PRACTICAL');for(const x of R.slice(0,12)){console.log(`\nLB${x.lb} sweep=${x.sweep}ATR wick=${x.wick} prior=${x.prior}ATR UTC${x.session[0]}-${x.session[1]} | ${x.pass?'PASS':'FAIL'}`);console.log('ALL   '+F(x.all));console.log('EARLY '+F(x.s1));console.log('MID   '+F(x.s2));console.log('LATE  '+F(x.s3))}const p=R.filter(x=>x.pass);console.log(`\n✅ PRACTICAL PASS: ${p.length}/${R.length}`);if(p.length){const x=p[0];console.log(`🥇 BEST PRACTICAL: LB${x.lb} sweep=${x.sweep} wick=${x.wick} prior=${x.prior} UTC${x.session[0]}-${x.session[1]}`);console.log(F(x.all))}console.log('\n📌 Goal: frequency + stability, not maximum historical WR. Fixed TP/SL $5/$5, max hold 20m. Research only; live untouched.');})().catch(e=>{console.error(e);process.exit(1)});
#!/usr/bin/env node
'use strict';
const {getHistoricalRates}=require('dukascopy-node');
const FROM=process.argv[2]||'2025-08-24',TO=process.argv[3]||'2026-08-24',CACHE='./data/dukascopy-cache';
function ema(v,p){const o=Array(v.length).fill(null),k=2/(p+1);let e=v[0];for(let i=0;i<v.length;i++){if(i)e=v[i]*k+e*(1-k);if(i>=p-1)o[i]=e}return o}
function atr(c,p=14){const o=Array(c.length).fill(null);let a=null;for(let i=1;i<c.length;i++){const tr=Math.max(c[i].high-c[i].low,Math.abs(c[i].high-c[i-1].close),Math.abs(c[i].low-c[i-1].close));if(i===p){let s=0;for(let j=1;j<=p;j++)s+=Math.max(c[j].high-c[j].low,Math.abs(c[j].high-c[j-1].close),Math.abs(c[j].low-c[j-1].close));a=s/p;o[i]=a}else if(i>p){a=(a*(p-1)+tr)/p;o[i]=a}}return o}
function st(t){let e=0,p=0,d=0,g=0,l=0,w=0,ls=0,ml=0,b=0;for(const x of t){e+=x.r;p=Math.max(p,e);d=Math.max(d,p-e);b+=x.bars;if(x.r>0){w++;g+=x.r;ls=0}else{l-=x.r;ls++;ml=Math.max(ml,ls)}}return{n:t.length,wr:t.length?w/t.length*100:0,net:e,pf:l?g/l:(g?999:0),dd:d,ls:ml,min:t.length?b/t.length*5:0}}
const F=s=>`${s.n} trades | WR ${s.wr.toFixed(1)}% | Net ${s.net>=0?'+':''}${s.net.toFixed(2)}R | PF ${s.pf.toFixed(2)} | DD ${s.dd.toFixed(2)}R | LS ${s.ls} | Avg ${s.min.toFixed(1)}m`;
(async()=>{console.log('⚡ GOLD FAST SCALP 5×5 — DISCOVERY');console.log(`📅 ${FROM} → ${TO}`);console.log('🎯 FIXED TP $5 | 🛑 FIXED SL $5 | BUY + SELL | research only');
const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(FROM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m5',format:'json',priceType:'bid',volumes:true,useCache:true,cacheFolderPath:CACHE});const c=raw.map(x=>({timestamp:+x.timestamp,open:+x.open,high:+x.high,low:+x.low,close:+x.close})).sort((a,b)=>a.timestamp-b.timestamp),cl=c.map(x=>x.close),e20=ema(cl,20),e50=ema(cl,50),A=atr(c);console.log(`✅ M5 ${c.length}`);
const all=[];for(const mode of ['PULLBACK','BREAKOUT'])for(const session of [[6,10],[7,12],[8,14],[11,15],[12,17],[13,18],[6,18]])for(const atrMin of [1.5,2,2.5,3])for(const bodyMin of [.45,.55,.65])for(const maxBars of [2,3,4,6]){let T=[];for(let i=60;i<c.length-maxBars-1;i++){if(![e20[i],e50[i],A[i]].every(Number.isFinite)||A[i]<atrMin)continue;const h=new Date(c[i].timestamp).getUTCHours();if(h<session[0]||h>session[1])continue;const b=c[i],range=b.high-b.low;if(!(range>0)||Math.abs(b.close-b.open)/range<bodyMin)continue;let side=null;if(mode==='PULLBACK'){const up=e20[i]>e50[i]&&e20[i]>e20[i-3],dn=e20[i]<e50[i]&&e20[i]<e20[i-3];if(up&&b.low<=e20[i]&&b.close>e20[i]&&b.close>b.open)side='BUY';if(dn&&b.high>=e20[i]&&b.close<e20[i]&&b.close<b.open)side='SELL'}else{let hi=-Infinity,lo=Infinity;for(let k=i-6;k<i;k++){hi=Math.max(hi,c[k].high);lo=Math.min(lo,c[k].low)}if(b.close>hi&&e20[i]>e50[i])side='BUY';if(b.close<lo&&e20[i]<e50[i])side='SELL'}if(!side)continue;const entry=c[i+1].open,tp=side==='BUY'?entry+5:entry-5,sl=side==='BUY'?entry-5:entry+5;let r=null,bars=maxBars;for(let j=i+1;j<=i+maxBars;j++){const hitSL=side==='BUY'?c[j].low<=sl:c[j].high>=sl,hitTP=side==='BUY'?c[j].high>=tp:c[j].low<=tp;if(hitSL||hitTP){r=hitSL?-1:1;bars=j-i;break}}if(r===null){const px=c[i+maxBars].close;r=(side==='BUY'?(px-entry):(entry-px))/5;r=Math.max(-1,Math.min(1,r))}T.push({time:c[i+1].timestamp,side,r,bars});i+=Math.max(0,bars-1)}const cut=Math.floor(T.length*.7),dev=st(T.slice(0,cut)),oos=st(T.slice(cut)),tot=st(T);let fail=[];if(tot.n<80)fail.push('N<80');if(dev.net<=0)fail.push('DEV-');if(oos.net<=0)fail.push('OOS-');if(dev.pf<1.1)fail.push('DEV_PF');if(oos.pf<1.1)fail.push('OOS_PF');if(tot.dd>20)fail.push('DD');const score=Math.min(dev.pf,3)*20+Math.min(oos.pf,3)*20+tot.net-tot.dd*1.5;all.push({mode,session,atrMin,bodyMin,maxBars,T,tot,dev,oos,fail:fail.join(',')||'PASS',score})}
all.sort((a,b)=>b.score-a.score);console.log(`🔬 Tested ${all.length} configs\n`);for(const x of all.slice(0,20)){console.log(`${x.mode} | UTC ${x.session[0]}-${x.session[1]} | ATR>=${x.atrMin} | body>=${x.bodyMin} | max ${x.maxBars*5}m | ${x.fail}`);console.log(' ALL '+F(x.tot));console.log(' DEV '+F(x.dev));console.log(' OOS '+F(x.oos));console.log(' BUY '+F(st(x.T.filter(t=>t.side==='BUY'))));console.log(' SELL '+F(st(x.T.filter(t=>t.side==='SELL')))+'\n')}
const pass=all.filter(x=>x.fail==='PASS');console.log(`✅ ROBUSTNESS PASS: ${pass.length}/${all.length}`);console.log('📌 Same-bar TP+SL conflict is conservatively counted as SL. Time exits are marked-to-market and capped to ±1R. Spread/slippage excluded at discovery stage.');})().catch(e=>{console.error(e);process.exit(1)});

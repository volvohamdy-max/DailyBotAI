'use strict';
// GOLD RANGE MR V3 — READ-ONLY ADX/ATR gate optimizer. Live logic untouched.
const fs=require('fs'),path=require('path');let raw=JSON.parse(fs.readFileSync(path.join(__dirname,'../data/xauusd-m5-dukascopy.json'),'utf8'));if(!Array.isArray(raw))raw=raw.candles||raw.data||raw.values||[];const n=v=>Number(v);let c=raw.map(x=>({t:n(x.timestamp??x.time??x.datetime??x.date),o:n(x.open??x.o),h:n(x.high??x.h),l:n(x.low??x.l),cl:n(x.close??x.c)})).filter(x=>[x.t,x.o,x.h,x.l,x.cl].every(Number.isFinite)).sort((a,b)=>a.t-b.t).slice(-50000);if(c[0]?.t<1e12)c.forEach(x=>x.t*=1000);
const B={emaSlopeMax:.42,widthMin:2.6,widthMax:5.8,edgeAtr:.28,edgeWidth:.075,wickMin:.35,bodyMax:.58,rsiEdge:45,slMinAtr:.90,slCapAtr:1.45,stopPadAtr:.22,minRR:.85,maxRR:1.35,maxBars:18};
function ema(v,p){let z=Array(v.length).fill(NaN),e=v[0],k=2/(p+1);for(let i=0;i<v.length;i++){if(i)e=v[i]*k+e*(1-k);if(i>=p-1)z[i]=e}return z}function rsi(v,p=14){let z=Array(v.length).fill(NaN),ag,al;for(let i=1;i<v.length;i++){let d=v[i]-v[i-1],g=Math.max(d,0),l=Math.max(-d,0);if(i===p){let G=0,L=0;for(let j=1;j<=p;j++){let q=v[j]-v[j-1];G+=Math.max(q,0);L+=Math.max(-q,0)}ag=G/p;al=L/p}else if(i>p){ag=(ag*(p-1)+g)/p;al=(al*(p-1)+l)/p}if(i>=p)z[i]=al===0?100:100-100/(1+ag/al)}return z}function atr(a,p=14){let z=Array(a.length).fill(NaN);for(let i=p;i<a.length;i++){let s=0;for(let j=i-p+1;j<=i;j++){let pc=a[j-1].cl;s+=Math.max(a[j].h-a[j].l,Math.abs(a[j].h-pc),Math.abs(a[j].l-pc))}z[i]=s/p}return z}function adx(a,p=14){let z=Array(a.length).fill(NaN),tr=Array(a.length).fill(0),pd=Array(a.length).fill(0),md=Array(a.length).fill(0);for(let i=1;i<a.length;i++){let u=a[i].h-a[i-1].h,d=a[i-1].l-a[i].l;pd[i]=u>d&&u>0?u:0;md[i]=d>u&&d>0?d:0;tr[i]=Math.max(a[i].h-a[i].l,Math.abs(a[i].h-a[i-1].cl),Math.abs(a[i].l-a[i-1].cl))}let T=0,P=0,M=0;for(let i=1;i<=p;i++){T+=tr[i];P+=pd[i];M+=md[i]}let dx=Array(a.length).fill(NaN);for(let i=p;i<a.length;i++){if(i>p){T=T-T/p+tr[i];P=P-P/p+pd[i];M=M-M/p+md[i]}if(T){let x=100*P/T,y=100*M/T;if(x+y)dx[i]=100*Math.abs(x-y)/(x+y)}}let seed=0,k=0;for(let i=p;i<a.length;i++)if(Number.isFinite(dx[i])){if(k<p){seed+=dx[i];k++;if(k===p)z[i]=seed/p}else if(Number.isFinite(z[i-1]))z[i]=(z[i-1]*(p-1)+dx[i])/p}return z}
let E=ema(c.map(x=>x.cl),20),R=rsi(c.map(x=>x.cl)),A=atr(c),D=adx(c),cut=Math.floor(c.length*.75);
function test(adxMax,atrLo,atrHi){let tr=[];for(let i=100;i<c.length-19;i++){if(![E[i],E[i-6],R[i],A[i],D[i]].every(Number.isFinite)||A[i]<=0||D[i]>adxMax)continue;let sm=A.slice(i-50,i).filter(Number.isFinite);if(sm.length<45)continue;let ratio=A[i]/(sm.reduce((a,b)=>a+b,0)/sm.length);if(ratio<atrLo||ratio>atrHi)continue;let slope=Math.abs(E[i]-E[i-6])/A[i];if(slope>B.emaSlopeMax)continue;let look=c.slice(i-30,i),hi=Math.max(...look.map(x=>x.h)),lo=Math.min(...look.map(x=>x.l)),width=hi-lo;if(width<A[i]*B.widthMin||width>A[i]*B.widthMax)continue;let edge=Math.max(A[i]*B.edgeAtr,width*B.edgeWidth),lt=0,ht=0;for(let b of look){if(b.l<=lo+edge)lt++;if(b.h>=hi-edge)ht++}if(lt<2||ht<2||look.slice(-6).some(b=>b.cl<lo-A[i]*.10||b.cl>hi+A[i]*.10))continue;let b=c[i],br=Math.max(1e-9,b.h-b.l),body=Math.abs(b.cl-b.o)/br,lw=(Math.min(b.o,b.cl)-b.l)/br,uw=(b.h-Math.max(b.o,b.cl))/br,mid=(hi+lo)/2,side=null;if(b.l<=lo+edge&&b.cl>=lo+edge*.75&&lw>=B.wickMin&&body<=B.bodyMax&&R[i]<=B.rsiEdge&&b.cl>lo&&b.cl<mid)side='BUY';if(!side&&b.h>=hi-edge&&b.cl<=hi-edge*.75&&uw>=B.wickMin&&body<=B.bodyMax&&R[i]>=100-B.rsiEdge&&b.cl<hi&&b.cl>mid)side='SELL';if(!side)continue;let entry=c[i+1].o,struct=side==='BUY'?Math.max(A[i]*B.slMinAtr,entry-(lo-A[i]*B.stopPadAtr)):Math.max(A[i]*B.slMinAtr,(hi+A[i]*B.stopPadAtr)-entry),sl=Math.min(struct,A[i]*B.slCapAtr),md=side==='BUY'?mid-entry:entry-mid,td=Math.min(md,sl*B.maxRR),rr=td/sl;if(!(sl>0&&rr>=B.minRR&&rr<=B.maxRR))continue;let stop=side==='BUY'?entry-sl:entry+sl,tp=side==='BUY'?entry+td:entry-td,out=null,end=Math.min(i+B.maxBars,c.length-1);for(let j=i+1;j<=end;j++){let hs=side==='BUY'?c[j].l<=stop:c[j].h>=stop,ht=side==='BUY'?c[j].h>=tp:c[j].l<=tp;if(hs&&ht){out=-1;break}if(hs){out=-1;break}if(ht){out=rr;break}}if(out===null){let px=c[end].cl;out=(side==='BUY'?(px-entry):(entry-px))/sl}tr.push({i,r:out})}return tr}
function st(a){let gp=a.filter(x=>x.r>0).reduce((s,x)=>s+x.r,0),gl=-a.filter(x=>x.r<0).reduce((s,x)=>s+x.r,0),w=a.filter(x=>x.r>0).length,net=a.reduce((s,x)=>s+x.r,0),eq=0,pk=0,dd=0,ls=0,mx=0;for(let x of a){eq+=x.r;pk=Math.max(pk,eq);dd=Math.max(dd,pk-eq);if(x.r<0){ls++;mx=Math.max(mx,ls)}else ls=0}return{t:a.length,wr:a.length?100*w/a.length:0,pf:gl?gp/gl:99,net,dd,ls:mx}}
let rows=[];for(let adxMax of [20,22,24,26,28,30])for(let atrLo of [.60,.65,.70,.72])for(let atrHi of [1.12,1.18,1.24,1.30]){if(atrLo>=atrHi)continue;let t=test(adxMax,atrLo,atrHi),d=st(t.filter(x=>x.i<cut)),h=st(t.filter(x=>x.i>=cut));if(d.t<50||h.t<15)continue;let score=d.net-d.dd*1.2+(d.pf-1)*8+h.net-h.dd+(h.pf-1)*10;rows.push({adxMax,atrLo,atrHi,d,h,score})}rows.sort((a,b)=>b.score-a.score);console.log('🌊 GOLD RANGE MR V3 — ADX/ATR OPTIMIZER (READ ONLY)');console.log(`Candles ${c.length} | ${rows.length} qualified combinations | DEV75 / HOLD25`);console.log('Only ADX max + ATR ratio band are varied. ALL other live conditions unchanged.');console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');rows.slice(0,30).forEach((x,i)=>console.log(`${String(i+1).padStart(2)} | ADX≤${x.adxMax} ATR${x.atrLo.toFixed(2)}-${x.atrHi.toFixed(2)} | DEV T${x.d.t} WR${x.d.wr.toFixed(1)} PF${x.d.pf.toFixed(2)} N${x.d.net.toFixed(1)}R DD${x.d.dd.toFixed(1)} LS${x.d.ls} | HOLD T${x.h.t} WR${x.h.wr.toFixed(1)} PF${x.h.pf.toFixed(2)} N${x.h.net.toFixed(1)}R DD${x.h.dd.toFixed(1)}`));console.log('━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━');console.log('BASELINE live = ADX≤20 ATR0.72-1.12');console.log('🛡️ LIVE UNCHANGED — optimizer only.');
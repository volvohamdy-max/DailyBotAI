#!/usr/bin/env node
'use strict';
/** Fixed validation only — NO optimization. Candidate selected previously. */
const {getHistoricalRates}=require('dukascopy-node');
const {atrSeries}=require('../src/services/scalpStrategies/goldRangeMrStrategy');
const CACHE='./data/dukascopy-cache',FROM='2025-08-30',TO='2026-08-29',F=Date.parse(FROM+'T00:00:00Z'),T=Date.parse(TO+'T23:59:59Z'),CUT=Date.parse('2026-05-30T00:00:00Z'),WARM='2025-06-01';
const P={bb:2.2,sb:2.6,bw:.3,sw:.25,cf:.15,gap:.3,tp:.6};
function st(a){let net=0,pk=0,dd=0,gp=0,gl=0,w=0,ls=0,ml=0;for(const x of a){net+=x.r;if(x.r>0){w++;gp+=x.r;ls=0}else{gl-=x.r;ml=Math.max(ml,++ls)}pk=Math.max(pk,net);dd=Math.max(dd,pk-net)}return{t:a.length,w,l:a.length-w,wr:a.length?100*w/a.length:0,pf:gl?gp/gl:(gp?999:0),net,dd,ls:ml}}
function run(c,A){const o=[];let busy=-1;for(let i=40;i<c.length-5;i++){if(i<=busy||!(A[i]>0))continue;const h=new Date(c[i].timestamp).getUTCHours();if(h<4||h>20)continue;const d=c[i-1].close-c[i-3].close;if(!d)continue;const side=d<0?'BUY':'SELL',burst=side==='BUY'?P.bb:P.sb,wm=side==='BUY'?P.bw:P.sw,risk=A[i]*(side==='BUY'?1:.75);if(Math.abs(d)<A[i]*burst)continue;let ag=0;for(let k=i-3;k<i;k++){if(d>0&&c[k].close>c[k].open)ag++;if(d<0&&c[k].close<c[k].open)ag++}if(ag<2)continue;const ex=c[i],rg=ex.high-ex.low;if(!(rg>0))continue;const uw=(ex.high-Math.max(ex.open,ex.close))/rg,lw=(Math.min(ex.open,ex.close)-ex.low)/rg;if(side==='BUY'&&lw<wm||side==='SELL'&&uw<wm)continue;const cf=c[i+1];if(side==='BUY'&&cf.close<=ex.low+rg*P.cf)continue;if(side==='SELL'&&cf.close>=ex.high-rg*P.cf)continue;const en=c[i+2].open;if(Math.abs(en-cf.close)>A[i]*P.gap)continue;const rew=A[i]*P.tp,sl=side==='BUY'?en-risk:en+risk,tp=side==='BUY'?en+rew:en-rew;let done=false;for(let j=i+2;j<=Math.min(c.length-1,i+4);j++){const loss=side==='BUY'?c[j].low<=sl:c[j].high>=sl,win=side==='BUY'?c[j].high>=tp:c[j].low<=tp;if(loss||win){if(c[i+2].timestamp>=F&&c[i+2].timestamp<=T)o.push({time:c[i+2].timestamp,r:loss?-1:rew/risk,side});busy=j;done=true;break}}if(!done){const j=Math.min(c.length-1,i+4),z=(side==='BUY'?c[j].close-en:en-c[j].close)/risk;if(c[i+2].timestamp>=F&&c[i+2].timestamp<=T)o.push({time:c[i+2].timestamp,r:Math.max(-1,Math.min(rew/risk,z)),side});busy=j}}return o}
const line=(n,a)=>{const s=st(a);console.log(`${n.padEnd(14)} T${String(s.t).padStart(3)} W${String(s.w).padStart(3)} L${String(s.l).padStart(3)} | WR ${s.wr.toFixed(1)}% | PF ${s.pf.toFixed(2)} | Net ${s.net>=0?'+':''}${s.net.toFixed(2)}R | DD ${s.dd.toFixed(2)}R | LS ${s.ls}`)};
(async()=>{console.log('⚡ E2 FIXED CANDIDATE VALIDATION — NO OPTIMIZER');console.log('P',P);const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(WARM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m5',format:'json',volumes:true,batchSize:10,pauseBetweenBatchesMs:0,useCache:true,cacheFolderPath:CACHE});const c=raw.map(x=>({timestamp:+new Date(x.timestamp),open:+x.open,high:+x.high,low:+x.low,close:+x.close})).filter(x=>[x.timestamp,x.open,x.high,x.low,x.close].every(Number.isFinite)).sort((a,b)=>a.timestamp-b.timestamp),A=atrSeries(c,14),a=run(c,A);console.log(`Data M5 ${c.length}`);line('FULL YEAR',a);line('PRE-3M',a.filter(x=>x.time<CUT));line('LAST-3M',a.filter(x=>x.time>=CUT));line('BUY',a.filter(x=>x.side==='BUY'));line('SELL',a.filter(x=>x.side==='SELL'));line('2025',a.filter(x=>new Date(x.time).getUTCFullYear()===2025));line('2026',a.filter(x=>new Date(x.time).getUTCFullYear()===2026));})();

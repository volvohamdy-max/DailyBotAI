const { getHistoricalRates }=require('dukascopy-node');
const FROM=process.argv[2]||'2025-08-24',TO=process.argv[3]||'2026-08-24';
const P={rr:2,breakAtr:.05,bodyMin:.5,closePos:.72,h1Slope:0,h1Sep:.08,rangeMax:2.2,emaDistMax:1.5,slAtr:.65,riskCap:1.35,maxBars:8};
function tf(a,min){const ms=min*60000,m=new Map();for(const x of a){const t=Math.floor(x.timestamp/ms)*ms;if(!m.has(t))m.set(t,{timestamp:t,open:x.open,high:x.high,low:x.low,close:x.close});else{const b=m.get(t);b.high=Math.max(b.high,x.high);b.low=Math.min(b.low,x.low);b.close=x.close}}return[...m.values()]}
function ema(v,p){const o=Array(v.length).fill(null),k=2/(p+1);let e=v[0];for(let i=0;i<v.length;i++){if(i)e=v[i]*k+e*(1-k);if(i>=p-1)o[i]=e}return o}
function atr(c,p=14){const o=Array(c.length).fill(null);let s=0;for(let i=1;i<c.length;i++){const tr=Math.max(c[i].high-c[i].low,Math.abs(c[i].high-c[i-1].close),Math.abs(c[i].low-c[i-1].close));s+=tr;if(i>p){const j=i-p,old=Math.max(c[j].high-c[j].low,Math.abs(c[j].high-c[j-1].close),Math.abs(c[j].low-c[j-1].close));s-=old}if(i>=p)o[i]=s/p}return o}
function prev(a,t){let l=0,h=a.length;while(l<h){const m=(l+h)>>1;if(a[m].timestamp<t)l=m+1;else h=m}return l-1}
function stats(t){let net=0,pk=0,dd=0,gp=0,gl=0,w=0,ls=0,ml=0;for(const x of t){net+=x.r;if(x.r>0){w++;gp+=x.r;ls=0}else{gl-=x.r;ml=Math.max(ml,++ls)}pk=Math.max(pk,net);dd=Math.max(dd,pk-net)}return{n:t.length,wr:t.length?100*w/t.length:0,net,pf:gl?gp/gl:999,dd,ls:ml}}
const fmt=s=>`${s.n} trades | WR ${s.wr.toFixed(1)}% | Net ${s.net>=0?'+':''}${s.net.toFixed(2)}R | Avg ${(s.n?s.net/s.n:0).toFixed(3)}R | PF ${s.pf.toFixed(2)} | DD ${s.dd.toFixed(2)}R | LS ${s.ls}`;
function base(c,h1){const C=c.map(x=>x.close),E20=ema(C,20),A=atr(c),HC=h1.map(x=>x.close),H20=ema(HC,20),H50=ema(HC,50),HA=atr(h1);let busy=-1;const out=[];for(let i=60;i<c.length-2;i++){if(i<=busy||!A[i])continue;const hour=new Date(c[i].timestamp).getUTCHours();if(hour<11||hour>17)continue;const h=prev(h1,c[i].timestamp);if(h<55||!HA[h])continue;const sep=Math.abs(H20[h]-H50[h])/HA[h];if(sep<P.h1Sep)continue;const hb=HC[h]>H20[h]&&H20[h]>H50[h]&&H20[h]>H20[h-2]?'BUY':HC[h]<H20[h]&&H20[h]<H50[h]&&H20[h]<H20[h-2]?'SELL':null;if(!hb)continue;let hi=-Infinity,lo=Infinity;for(let j=i-3;j<i;j++){hi=Math.max(hi,c[j].high);lo=Math.min(lo,c[j].low)}const range=c[i].high-c[i].low,body=Math.abs(c[i].close-c[i].open);if(!range||body/A[i]<P.bodyMin||range/A[i]>P.rangeMax)continue;const pos=(c[i].close-c[i].low)/range;if(Math.abs(c[i].close-E20[i])/A[i]>P.emaDistMax)continue;const buy=hb==='BUY'&&c[i].close>hi+P.breakAtr*A[i]&&pos>=P.closePos&&c[i].close>E20[i],sell=hb==='SELL'&&c[i].close<lo-P.breakAtr*A[i]&&pos<=1-P.closePos&&c[i].close<E20[i];if(!buy&&!sell)continue;const side=buy?'BUY':'SELL',en=c[i+1].open;if(Math.abs(en-c[i].close)>A[i]*.3)continue;const swing=side==='BUY'?Math.min(c[i].low,c[i-1].low):Math.max(c[i].high,c[i-1].high),risk=Math.max(A[i]*P.slAtr,Math.abs(en-swing));if(risk>A[i]*P.riskCap)continue;const sl=side==='BUY'?en-risk:en+risk,tp=side==='BUY'?en+risk*P.rr:en-risk*P.rr;let done=false;for(let j=i+1;j<=Math.min(c.length-1,i+P.maxBars);j++){const loss=side==='BUY'?c[j].low<=sl:c[j].high>=sl,win=side==='BUY'?c[j].high>=tp:c[j].low<=tp;if(loss||win){out.push({time:c[i+1].timestamp,r:loss?-1:P.rr,side,hour});busy=j;done=true;break}}if(!done){const j=Math.min(c.length-1,i+P.maxBars),r=(side==='BUY'?c[j].close-en:en-c[j].close)/risk;out.push({time:c[i+1].timestamp,r:Math.max(-1,Math.min(P.rr,r)),side,hour});busy=j}}return out}
function report(name,t,cut){const all=stats(t),dev=stats(t.filter(x=>x.time<cut)),oos=stats(t.filter(x=>x.time>=cut));console.log(`\n━━━━━━━━ ${name} ━━━━━━━━`);console.log('ALL '+fmt(all));console.log('DEV '+fmt(dev));console.log('OOS '+fmt(oos));return{all,dev,oos}}
(async()=>{console.log('⚡ GOLD RAPID SCALP V5 — VALIDATION ONLY');console.log(`📅 ${FROM} → ${TO}`);console.log('🔒 Frozen V4 parameters:',P);const raw=await getHistoricalRates({instrument:'xauusd',dates:{from:new Date(FROM+'T00:00:00Z'),to:new Date(TO+'T23:59:59Z')},timeframe:'m5',format:'json',volumes:true,batchSize:10,pauseBetweenBatchesMs:300,useCache:true,cacheFolderPath:'./data/dukascopy-cache'});const c=raw.map(x=>({timestamp:+x.timestamp,open:+x.open,high:+x.high,low:+x.low,close:+x.close})).filter(x=>Object.values(x).every(Number.isFinite)).sort((a,b)=>a.timestamp-b.timestamp),h1=tf(c,60),t=base(c,h1),cut=Date.UTC(2026,0,1);console.log(`✅ M5 ${c.length} | H1 ${h1.length} | base trades ${t.length}`);const variants=[['A BASE V4',t],['B BUY ONLY',t.filter(x=>x.side==='BUY')],['C EXCLUDE 16 UTC',t.filter(x=>x.hour!==16)],['D BUY ONLY + EXCLUDE 16 UTC',t.filter(x=>x.side==='BUY'&&x.hour!==16)]];const results=variants.map(([n,z])=>[n,report(n,z,cut)]);console.log('\n📅 YEAR SPLIT');for(const [n,z] of variants){console.log(`\n${n}`);for(const y of [2025,2026])console.log(`${y} | ${fmt(stats(z.filter(x=>new Date(x.time).getUTCFullYear()===y)))}`)}console.log('\n📆 MONTHLY — FINAL CANDIDATE D');const d=variants[3][1];for(let y=2025;y<=2026;y++)for(let m=0;m<12;m++){const z=d.filter(x=>{const q=new Date(x.time);return q.getUTCFullYear()===y&&q.getUTCMonth()===m});if(z.length)console.log(`${y}-${String(m+1).padStart(2,'0')} | ${fmt(stats(z))}`)}console.log('\n🕐 HOUR SPLIT — BUY ONLY');const b=variants[1][1];for(let h=11;h<=17;h++)console.log(`${String(h).padStart(2,'0')}:00 UTC | ${fmt(stats(b.filter(x=>x.hour===h)))}`);console.log('\n🧪 VALIDATION DECISION');for(const [n,r] of results){const pass=r.dev.n>=30&&r.oos.n>=50&&r.dev.net>0&&r.oos.net>0&&r.dev.pf>=1.30&&r.oos.pf>=1.30&&r.all.pf>=1.30&&r.all.dd<=12;console.log(`${pass?'✅':'⚠️'} ${n} | ${pass?'PASS':'FAIL'} strict gates`)}console.log('\n⚠️ Validation only — live strategies and VIP routing untouched.');})().catch(e=>{console.error(e);process.exit(1)});

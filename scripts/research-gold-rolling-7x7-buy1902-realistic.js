#!/usr/bin/env node
'use strict';
// RESEARCH ONLY — realistic validation: BUY only 19:00→02:00 UTC, one active trade at a time.
// Tests several round-trip execution-cost assumptions. Does NOT touch live strategies.
const fs=require('fs'),path=require('path');
const DATA=path.join(__dirname,'../data/backtests/history-cache/XAUUSD_5min.json');
const raw=JSON.parse(fs.readFileSync(DATA,'utf8'));
const c=(Array.isArray(raw)?raw:(raw.candles||raw.data||raw.items||[])).map(x=>({t:+(x.timestamp??x.time??x.t),o:+x.open,h:+x.high,l:+x.low,c:+x.close})).filter(x=>[x.t,x.o,x.h,x.l,x.c].every(Number.isFinite)).sort((a,b)=>a.t-b.t);
if(c.length<100)throw new Error('Not enough candles');
const MAX_HOLD=24,RANGE_BARS=3;function ema(p){let a=[],x=c[0].c,k=2/(p+1);for(let i=0;i<c.length;i++){x=i?c[i].c*k+x*(1-k):x;a[i]=x}return a}function atr(p=14){let o=[],s=0,tr=[];for(let i=1;i<c.length;i++){tr[i]=Math.max(c[i].h-c[i].l,Math.abs(c[i].h-c[i-1].c),Math.abs(c[i].l-c[i-1].c));s+=tr[i];if(i>p)s-=tr[i-p];if(i>=p)o[i]=s/p}return o}function rsi(p=14){let o=[],g=0,l=0;for(let i=1;i<=p;i++){let d=c[i].c-c[i-1].c;d>=0?g+=d:l-=d}g/=p;l/=p;for(let i=p;i<c.length;i++){if(i>p){let d=c[i].c-c[i-1].c;g=(g*(p-1)+Math.max(d,0))/p;l=(l*(p-1)+Math.max(-d,0))/p}o[i]=100-100/(1+(l?g/l:999))}return o}function adx(p=14){let tr=[],pl=[],mi=[],o=[];for(let i=1;i<c.length;i++){tr[i]=Math.max(c[i].h-c[i].l,Math.abs(c[i].h-c[i-1].c),Math.abs(c[i].l-c[i-1].c));let u=c[i].h-c[i-1].h,d=c[i-1].l-c[i].l;pl[i]=u>d&&u>0?u:0;mi[i]=d>u&&d>0?d:0}let ts=0,ps=0,ms=0,dx=[];for(let i=1;i<c.length;i++){if(i<=p){ts+=tr[i];ps+=pl[i];ms+=mi[i];if(i<p)continue}else{ts=ts-ts/p+tr[i];ps=ps-ps/p+pl[i];ms=ms-ms/p+mi[i]}let pdi=ts?100*ps/ts:0,mdi=ts?100*ms/ts:0;dx[i]=(pdi+mdi)?100*Math.abs(pdi-mdi)/(pdi+mdi):0;if(i>=2*p-1){if(i===2*p-1){let s=0;for(let j=p;j<=i;j++)s+=dx[j]||0;o[i]=s/p}else o[i]=(o[i-1]*(p-1)+dx[i])/p}}return o}
const E20=ema(20),E50=ema(50),A=atr(),R=rsi(),D=adx();function sess(h){return h>=19||h<2}
function generate(){const t=[];let nextFree=60;for(let i=60;i<c.length-MAX_HOLD-1;i++){if(i<nextFree)continue;let h=new Date(c[i].t).getUTCHours();if(!sess(h)||!A[i]||!D[i]||!R[i]||A[i]>7||D[i]<19)continue;let b=c[i];if(b.h-b.l>1.4*A[i])continue;let hi=-Infinity;for(let j=i-3;j<i;j++)hi=Math.max(hi,c[j].h);if(!(b.c>hi&&E20[i]>E50[i]&&b.c>E20[i]&&R[i]>52))continue;let en=c[i+1].o,sl=en-7,tp=en+7,r=0,exit=i+MAX_HOLD;for(let j=i+1;j<=i+MAX_HOLD;j++){let loss=c[j].l<=sl,win=c[j].h>=tp;if(loss||win){r=loss?-1:1;exit=j;break}}if(!r)r=Math.max(-1,Math.min(1,(c[exit].c-en)/7));t.push({raw:r,t:c[i].t,exitT:c[exit].t,hour:h,month:new Date(c[i].t).toISOString().slice(0,7)});nextFree=exit+1}return t}
function st(t,cost=0){let net=0,g=0,l=0,w=0,pk=0,dd=0,ls=0,cur=0;for(const x of t){let r=x.raw-cost/7;net+=r;pk=Math.max(pk,net);dd=Math.max(dd,pk-net);if(r>0){w++;g+=r;cur=0}else if(r<0){l-=r;cur++;ls=Math.max(ls,cur)}}return{n:t.length,wr:t.length?100*w/t.length:0,pf:l?g/l:(g?999:0),net,dd,ls}}
function fmt(s){return `T${s.n} WR${s.wr.toFixed(1)}% PF${s.pf.toFixed(2)} Net${s.net.toFixed(1)}R DD${s.dd.toFixed(1)}R LS${s.ls}`}
const t=generate(),cutT=c[0].t+(c[c.length-1].t-c[0].t)*.75,dev=t.filter(x=>x.t<cutT),hold=t.filter(x=>x.t>=cutT);
console.log(`🧪 GOLD 7x7 BUY 19→02 REALISTIC — RESEARCH ONLY | M5=${c.length}`);console.log(`Period: ${new Date(c[0].t).toISOString()} -> ${new Date(c[c.length-1].t).toISOString()}`);console.log('Rules unchanged + ONE ACTIVE TRADE ONLY. Cost below = assumed total round-trip price drag per trade.');
for(const cost of [0,0.10,0.20,0.30,0.50]){console.log(`\n💸 COST $${cost.toFixed(2)} / trade`);console.log('ALL  | '+fmt(st(t,cost)));console.log('DEV  | '+fmt(st(dev,cost)));console.log('HOLD | '+fmt(st(hold,cost)))}
console.log('\n📅 MONTHLY @ $0.20 cost');for(const m of [...new Set(t.map(x=>x.month))])console.log(`${m} | ${fmt(st(t.filter(x=>x.month===m),.20))}`);
console.log('\n🕐 HOURS @ $0.20 cost');for(const h of [19,20,21,22,23,0,1])console.log(`${String(h).padStart(2,'0')}:00 | ${fmt(st(t.filter(x=>x.hour===h),.20))}`);
